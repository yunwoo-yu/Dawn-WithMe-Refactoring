import styled from 'styled-components';
import AuthFormWrapper from '../../common/Wrapper/AuthFormWrapper';

const ProfileSettingFormWrapper = styled(AuthFormWrapper)`
  div {
    padding-top: 10px;
    margin-bottom: 6px;
  }

  p {
    margin: 6px 0;
  }
`;

export default ProfileSettingFormWrapper;
