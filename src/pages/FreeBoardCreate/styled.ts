import styled from 'styled-components';
import AuthWrapper from '../../components/common/Wrapper/AuthWrapper';

const FreeBoardCreateWrapper = styled(AuthWrapper)``;

export default FreeBoardCreateWrapper;
